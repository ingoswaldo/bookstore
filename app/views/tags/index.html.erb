<div class="row row-cols-1 row-cols-md-3 mb-3 text-center">
  <% @books.each do |book, tag| %>
    <%= render 'layouts/components/book-card', book: book, tag: { id: book.tag_id, name: book.tag_name }, show_more: true %>
  <% end %>
</div>

<%= paginate @books %>